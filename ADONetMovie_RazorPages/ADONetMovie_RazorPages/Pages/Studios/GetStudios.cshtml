@page
@model  ADONetMovie_RazorPages.Pages.Studios.GetStudiosModel
@{
    ViewData["Title"] = "GetStudios";
}

<h1>List of studios</h1>
<p>
    <a asp-page="/Studios/Create">Create New Studio</a>
</p>

<h4 style="color:red"> List of studios</h4>
<table class="table">
    <thead>
        <tr>
            <th>
                Id
            </th>
            <th>
                Name
            </th>
            <th>
                Hqcity
            </th>

            <th>
                Number of employee
            </th>
        </tr>
    </thead>
    <tbody>

        @if (Model != null)
        {
            @foreach (var item in Model.Studios)
            {
        <tr>
            <td>
                @item.Id
            </td>
            <td>
                @item.Name
            </td>
            <td>
                @item.Hqcity
            </td>
            <td>
                @item.NoOfEmployees
            </td>
            <td>
                <p>
                    <a asp-page="/Movies/GetMovies" asp-page-handler="MoviesByStudio" asp-route-sid="@item.Id"> Studios´s movies  </a>
                </p>
            </td>

            @*<td>
            <p>

                <a asp-page="/Sales/MySales" asp-route-id="@item.Id">Show my Sales</a>
            </p>
        </td>
        <td>
            <p>

                <a asp-page="/Sales/Create" asp-route-id="@item.CustomerId">New Sale</a>
            </p>
        </td>

        <td>
            <p>
                <a asp-page="/Customers/Delete" asp-route-id="@item.CustomerId">Delete</a>
            </p>
        </td>*@
        </tr>
            }
        }
</table>



